# Pinned to the versions the hosted demo was verified against.
# Unpinned ranges previously let matplotlib 3.11 through, which removed
# boxplot(labels=) and broke the dashboard in production but not locally.
prophet==1.4.0
streamlit==1.62.0
pandas==3.0.5
numpy==2.5.2
matplotlib==3.11.1
plotly==7.0.0
sqlalchemy==2.0.52
psycopg2-binary==2.9.12
requests==2.34.2
tqdm==4.70.0
fastapi==0.141.1
uvicorn==0.52.4
pydantic==2.13.5
pytest-cov==7.1.0
scikit-learn==1.9.0
xgboost==3.4.1
statsmodels==0.15.0
